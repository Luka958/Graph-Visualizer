const express = require('express');
//const addon = require('../build/Release/addon');
const router = express.Router();

router.post('/', (req, res) => {
    /** @property {Function} runDijkstra */
    throw new Error("not supported")
});

module.exports = router;